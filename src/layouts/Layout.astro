---
import ParticleBackground from '../components/ParticleBackground.jsx';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Arkatomy - Juego Arkanoid para Tómas" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<meta name="theme-color" content="#000000" />
		<meta name="apple-mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
		<title>{title}</title>
	</head>
	<body>
		<!-- Fondo animado con partículas neón -->
		<ParticleBackground client:load />

		<slot/>
		<div class="game-container">
			<div class="game-ui">
				<div class="score">Puntos: <span id="score-value">0</span></div>
				<div class="level">Nivel: <span id="level-value">1</span></div>
				<div class="lives">Vidas: <span id="lives-value">3</span></div>
			</div>
			<canvas id="game"></canvas>

			<!-- Controles móviles -->
			<div class="mobile-controls">
				<div class="control-row">
					<button id="btn-left" class="control-btn" aria-label="Mover izquierda">
						<svg viewBox="0 0 24 24" fill="currentColor"><path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"/></svg>
					</button>
					<button id="btn-start" class="control-btn start-btn" aria-label="Iniciar juego">
						START
					</button>
					<button id="btn-right" class="control-btn" aria-label="Mover derecha">
						<svg viewBox="0 0 24 24" fill="currentColor"><path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z"/></svg>
					</button>
				</div>
			</div>

			<p class="instructions desktop-only">
				Mueve el mouse o usa las flechas del teclado
			</p>
		</div>
		<img hidden id="bricks" src="/bricks.png" alt="bricks spritesheet" />
		<img hidden id="sprite" src="/sprite.png" alt="paddle spritesheet" />
		<script src="/main.js" defer></script>
	</body>
</html>

<style is:global>
	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	:root {
		--neon-green: #39ff14;
		--neon-yellow: greenyellow;
		--bg-dark: #0a0a0a;
	}

	html, body {
		height: 100%;
		overflow-x: hidden;
	}

	body {
		background-color: var(--bg-dark);
		font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		align-items: center;
		padding: 10px;
		touch-action: manipulation;
		-webkit-touch-callout: none;
		-webkit-user-select: none;
		user-select: none;
		position: relative;
		overflow: hidden;
	}

	h1 {
		text-align: center;
		color: var(--neon-yellow);
		font-size: 2.5em;
		margin-top: 20px;
		margin-bottom: 5px;
		text-shadow: 0 0 10px var(--neon-green), 0 0 20px var(--neon-green);
		position: relative;
		z-index: 1;
	}

	h2 {
		color: var(--neon-green);
		font-weight: bold;
		text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.8);
		text-align: center;
		font-size: 1.2em;
		margin-bottom: 15px;
		position: relative;
		z-index: 1;
	}

	.game-container {
		display: flex;
		flex-direction: column;
		align-items: center;
		width: 100%;
		max-width: 500px;
		position: relative;
		z-index: 1;
	}

	.game-ui {
		display: flex;
		justify-content: space-between;
		width: 100%;
		max-width: 448px;
		padding: 10px 5px;
		margin-bottom: 5px;
	}

	.score, .lives, .level {
		color: #fff;
		font-size: 1.1em;
		font-weight: bold;
		text-shadow: 0 0 5px var(--neon-green);
	}

	#score-value, #lives-value, #level-value {
		color: var(--neon-green);
	}

	canvas#game {
		border: 4px solid #fff;
		border-bottom: 4px solid var(--neon-green);
		background: url('bg1.png');
		background-repeat: repeat;
		display: block;
		max-width: 100%;
		height: auto;
		touch-action: none;
		box-shadow: 0 0 20px rgba(57, 255, 20, 0.3);
	}

	/* Controles móviles */
	.mobile-controls {
		display: none;
		width: 100%;
		max-width: 350px;
		margin-top: 20px;
		padding: 10px;
	}

	.control-row {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 20px;
	}

	.control-btn {
		width: 70px;
		height: 70px;
		border-radius: 50%;
		border: 3px solid var(--neon-green);
		background: rgba(57, 255, 20, 0.1);
		color: var(--neon-green);
		font-size: 1.2em;
		font-weight: bold;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: all 0.15s ease;
		box-shadow: 0 0 15px rgba(57, 255, 20, 0.3);
		-webkit-tap-highlight-color: transparent;
	}

	.control-btn svg {
		width: 40px;
		height: 40px;
	}

	.control-btn:active {
		background: rgba(57, 255, 20, 0.4);
		transform: scale(0.95);
		box-shadow: 0 0 25px rgba(57, 255, 20, 0.6);
	}

	.start-btn {
		width: 80px;
		height: 50px;
		border-radius: 25px;
		font-size: 0.9em;
		letter-spacing: 1px;
	}

	.start-btn.playing {
		border-color: #ff4444;
		color: #ff4444;
		background: rgba(255, 68, 68, 0.1);
		box-shadow: 0 0 15px rgba(255, 68, 68, 0.3);
	}

	.start-btn.playing::after {
		content: 'PAUSE';
	}

	.start-btn:not(.playing)::after {
		content: '';
	}

	.instructions {
		color: #888;
		font-size: 0.85em;
		text-align: center;
		margin-top: 15px;
		padding: 0 10px;
	}

	.desktop-only {
		display: block;
	}

	/* Tablet */
	@media (max-width: 768px) {
		h1 {
			font-size: 2em;
			margin-top: 15px;
		}

		h2 {
			font-size: 1em;
		}

	}

	/* Mobile */
	@media (max-width: 600px) {
		body {
			padding: 5px;
		}

		h1 {
			font-size: 1.8em;
			margin-top: 10px;
		}

		h2 {
			font-size: 0.9em;
			margin-bottom: 10px;
		}

		.game-ui {
			padding: 8px 10px;
		}

		.score, .lives {
			font-size: 1em;
		}

		.mobile-controls {
			display: block;
		}

		.desktop-only {
			display: none;
		}

	}

	/* Móviles pequeños */
	@media (max-width: 400px) {
		.control-btn {
			width: 60px;
			height: 60px;
		}

		.control-btn svg {
			width: 32px;
			height: 32px;
		}

		.start-btn {
			width: 70px;
			height: 45px;
			font-size: 0.8em;
		}

		.control-row {
			gap: 15px;
		}
	}

	/* Landscape mobile */
	@media (max-height: 500px) and (orientation: landscape) {
		h1 {
			font-size: 1.4em;
			margin-top: 5px;
			margin-bottom: 3px;
		}

		h2 {
			font-size: 0.8em;
			margin-bottom: 5px;
		}

		.game-ui {
			padding: 5px;
		}

		.mobile-controls {
			margin-top: 10px;
		}

		.control-btn {
			width: 50px;
			height: 50px;
		}

		.start-btn {
			width: 60px;
			height: 40px;
			font-size: 0.7em;
		}

	}

	/* Prevenir zoom en iOS */
	@media screen and (-webkit-min-device-pixel-ratio: 0) {
		input, select, textarea, button {
			font-size: 16px;
		}
	}
</style>
